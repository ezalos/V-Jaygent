name: coprime-wheels-turca
identity: "four coprime tooth-wheels around the star"
default_blend: max

uniforms:
  inner_radius: { type: float, default: 0.26 }
  outer_radius: { type: float, default: 0.62 }

reads:
  - u_below
