name: haze-drift
identity: "warm domain-warped haze, slow drift"
default_blend: screen

uniforms:
  haze_scale:    { type: float, default: 3.5 }
  haze_strength: { type: float, default: 0.30 }

reads:
  - u_below
