name: vignette-grain
identity: "warm vignette + 1/f film grain"
default_blend: replace

uniforms:
  vignette_strength: { type: float, default: 0.55 }
  grain_strength:    { type: float, default: 0.05 }

reads:
  - u_below
